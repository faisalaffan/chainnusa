{
  "name": "@chainnusa/contracts",
  "version": "0.2.0",
  "private": true,
  "description": "ChainNusa smart contracts — Hardhat + Foundry dual build",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.6.1"
  },
  "engines": {
    "node": ">=20"
  }
}
